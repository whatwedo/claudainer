http_port 3128

access_log stdio:/dev/stdout
cache_log /dev/stderr

# Future: acl allowed_sites dstdomain .anthropic.com .github.com
# Future: http_access allow CONNECT allowed_sites
# Future: http_access allow allowed_sites
# Future: http_access deny all

http_access allow all
